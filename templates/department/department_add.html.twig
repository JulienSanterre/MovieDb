{% extends 'base.html.twig' %}

{% block title %}Modification d'un département{% endblock %}

{% block body %}

{# Department FORM #}
<div class="container col-12">
    <div class="row col-12">
        {{ form_start(formDepartment, { attr: { class: 'log-form col-4'}}) }}

        <label for="inputDepartmentName">Nom</label>
        {{ form_widget(formDepartment.name, { attr: {  
                    autofocus: true,
                    id: "inputDepartmentName",
                    class: "form-control"
                }}) }}

        {{ form_widget(formDepartment.send, { attr: {
                    class: "btn btn-lg btn-dark log-form-btn col-12"
                }}) }}

        {{ form_end(formDepartment) }}
    </div>
</div>
{% endblock %}
