{% extends 'base.html.twig' %}

{% block title %}Modification d'un interprète{% endblock %}

{% block body %}

{# Person FORM #}
<div class="container col-12">
    <div class="row col-12">
        {{ form_start(formPerson, { attr: { class: 'log-form col-4'}}) }}

        <label for="inputPersonName">Nom</label>
        {{ form_widget(formPerson.name, { attr: {  
                    autofocus: true,
                    id: "inputPersonName",
                    class: "form-control"
                }}) }}
                
        {{ form_widget(formPerson.send, { attr: {
                    class: "btn btn-lg btn-dark log-form-btn col-12"
                }}) }}

        {{ form_end(formPerson) }}
    </div>
</div>
{% endblock %}
